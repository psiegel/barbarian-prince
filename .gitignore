# Game content - copyright Reaper Miniatures, not redistributed from this repo.
# Download the booklets yourself (see README) and regenerate the data locally.
pdfs/
data/*.json
data/map-data.csv
# errata.json, procedures.json, creatures.json and map-fixes.json are hand-written
# commentary, sequencing, band sizes and corrections, not extracted content, so
# they are tracked.
!data/errata.json
!data/procedures.json
!data/creatures.json
!data/map-fixes.json

# Generated speech
audio/

# One JSON file per playthrough - somebody's game in progress, not source
saves/

# Secrets
.env

.DS_Store
__pycache__/
